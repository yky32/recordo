import 'package:flutter/material.dart';

/// Uber-adjacent palette (not Uber brand assets).
abstract final class UberColors {
  static const black = Color(0xFF000000);
  static const sheet = Color(0xFF121212);
  static const elevated = Color(0xFF1C1C1E);
  static const elevated2 = Color(0xFF2C2C2E);
  static const white = Color(0xFFFFFFFF);
  static const muted = Color(0xFF8E8E93);
  static const hairline = Color(0xFF3A3A3C);
  /// Soft green status (session live) — not Uber trademark green lockup.
  static const accent = Color(0xFF06C167);
  static const danger = Color(0xFFFF3B30);
  static const pin = Color(0xFFFFFFFF);
  static const mapRoad = Color(0xFF1A1A1A);
  static const mapBlock = Color(0xFF0D0D0D);
  static const mapGrid = Color(0xFF242424);
}
